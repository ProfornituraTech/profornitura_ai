from setuptools import setup, find_packages

setup(
    name="profornitura_ai",
    version="10.0.0",
    description="Profornitura AI - Minimal deterministic foundation",
    author="Profornitura Italia SRL",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)
